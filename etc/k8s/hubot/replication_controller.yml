---
apiVersion: v1
kind: ReplicationController
metadata:
  name: hubot
  labels:
    app: hubot
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: hubot
    spec:
      containers:
      - name: hubot
        image: gcr.io/dev-europe-west1/hubot:3e952a0
        command: ["/hubot/bin/hubot-k8s"]
      volumes:
      - name: secrets
        secret:
          secretName: hubot
