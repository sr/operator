#!/usr/bin/env bash
set -eu
cd "$(dirname "$0")/../.."

if [ -z "${RUBOCOP_CONFIG-}" ]; then
  RUBOCOP_CONFIG="etc/rubocop.yml"
fi

exec rubocop \
  --config "$(pwd)/$RUBOCOP_CONFIG" \
  --format "emacs" \
  --fail-level "C" \
  --rails \
  --display-style-guide \
  --display-cop-names \
  "$@"
