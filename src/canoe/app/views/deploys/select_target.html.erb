<h1>
  <%= content_tag :i, "", class: "icon-#{current_repo.icon}" %>
  <%= current_repo.name.capitalize %>
</h1>

<h2 class="steps-count">Step 3 of 4</h2>
<h3>
  Where should we deploy this?
  <span class="muted">
    &nbsp;
    <%= print_deploy_what @prov_deploy %>
  </span>
</h3>
<br>

<p class="lead muted">
  Select the <i class="icon-bullseye"></i> for <span class='repo'><%= @prov_deploy.repo_name %></span> deployment.
</p>

<% @targets.each do |target| %>
  <%= render partial: "deploys/target", locals: { target: target }  %>
  <%# "<hr>" unless @targets.last == target %>
<% end %>
