version: "2"
services:
  redis:
    image: redis:3.0.7
    expose:
      - "6379"
  bot:
    build: .
    environment:
      REDIS_HOST: "redis"
      REDIS_PORT: "6379"
      LITA_ADAPTER: "nothing"
    ports:
      - "8082:8080"
    volumes:
      - .:/app
    links:
      - redis
