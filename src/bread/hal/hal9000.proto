syntax = "proto3";

package hal;

option go_package = "breadhal";

service Robot {
	rpc IsMatch(Message) returns (IsMatchResponse);
	rpc Dispatch(Message) returns (DispatchResponse);
}

message Message {
	string text = 1;
}

message IsMatchResponse {
	bool match = 1;
}

message DispatchResponse {
}
