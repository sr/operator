syntax = "proto3";

package hal9000;

option go_package = "hal9000";

service Robot {
	rpc IsMatch(Message) returns (Response);
	rpc Dispatch(Message) returns (Response);
}

message Message {
	string text = 1;
	string user_email = 2;
	string room = 3;
}

message Response {
	bool match = 1;
}
