#!/bin/sh
cd "$(dirname "$0")/.."
set -eu

GOPATH="$(pwd)/go"
export GOPATH
mkdir "$GOPATH"

git config --global url."https://sr:$GITHUB_REPO_TOKEN@github.com/".insteadOf "https://github.com/"

make vet errcheck build install
