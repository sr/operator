#!/bin/sh
set -eu
: "${GOROOT:="$(go env GOROOT)"}"
: "${PAGER:="less"}"

case "${1:-""}" in
    -http*)
        "${GOROOT}/bin/godoc" "$@"
        ;;
    *)
        "${GOROOT}/bin/godoc" "$@" | $PAGER
        ;;
esac
